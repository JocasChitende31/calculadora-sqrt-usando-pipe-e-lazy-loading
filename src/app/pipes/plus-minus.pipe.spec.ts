import { PlusMinusPipe } from './plus-minus.pipe';

describe('PlusMinusPipe', () => {
  it('create an instance', () => {
    const pipe = new PlusMinusPipe();
    expect(pipe).toBeTruthy();
  });
});
