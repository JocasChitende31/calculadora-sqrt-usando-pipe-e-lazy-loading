import { MultiplieTablePipe } from './multiplie-table.pipe';

describe('MultiplieTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplieTablePipe();
    expect(pipe).toBeTruthy();
  });
});
